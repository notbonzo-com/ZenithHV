/* Good Tests */
#pragma once

#include <stdint.h>
#include <stdbool.h>
#include <kprintf>
#include <sys/idt.hpp>

namespace gtest {

inline void assert_runtime(bool condition, const char* message) {
  if (!condition) {
    kprintf("\n\n\n -- Offending test: %s\n", message);
    intr::kpanic(NULL, "TEST FAILED");
  } else {
    kprintf(" -> TEST PASSED: %s\n", message);
  }
}

}